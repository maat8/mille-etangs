{% extends 'MilleEtangsRandonneesBundle::layout.html.twig' %}

{% block title %}{{ randonnee.getNom() }} | {{ parent()}}{% endblock %}

{% block body %}
	<h2>{{ randonnee.getNom() }}</h2>

	<p>{{ randonnee.getTeaser()|raw }}</p>

	<h5>Accès</h5>
	<p>{{ randonnee.getAcces()|raw }}</p>

	<h5>Description</h5>
	<p>{{ randonnee.getDescription()|raw }}</p>

	<p>Distance <strong>{{ (randonnee.getDistance()/1000) }} km</strong></p>

	{% if randonnee.getDureeVtt() > 0 %}
		<p>Durée VTT <strong>{{ randonnee.getDureeVtt() }} minutes</strong></p>
	{% endif %}

	{% if randonnee.getDureeMarche() > 0 %}
		<p>Durée à pied <strong>{{ randonnee.getDureeMarche() }} min</strong></p>
	{% endif %}

	{% if randonnee.getDureeCheval() > 0 %}
		<p>Durée à cheval <strong>{{ randonnee.getDureeCheval() }} min</strong></p>
	{% endif %}

	<p>Dénivelé positif <strong>{{ randonnee.getDenivelePositif() }} m</strong></p>

	{% if(randonnee.getEndomondoLink() is not null) %}
		<a href='{{ randonnee.getEndomondoLink() }}'>Endomondo</a>
	{% endif %}

{% endblock %}
